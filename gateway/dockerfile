FROM node:20-slim

WORKDIR /app

# Copy only package files first for better caching
COPY gateway/package*.json ./

# Install dependencies
RUN npm install

# Copy the rest of the gateway code
COPY gateway/ .

# Expose GraphQL Gateway port
EXPOSE 4000

# Start the gateway
CMD ["node", "gateway.js"]