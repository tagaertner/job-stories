FROM node:20-alpine

WORKDIR /app

# Copy package files from the correct path within build context
COPY gateway/package*.json ./
RUN npm install

# Copy the rest of the gateway files
COPY gateway/ .

ENV PORT=4100
EXPOSE 4100

CMD ["node", "gateway.js"]